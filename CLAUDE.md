@AGENTS.md

## Claude Code

Project configuration lives in `.claude/`: pre-approved and denied commands in
`settings.json`, a formatting hook, a documentation rule that loads when you touch
Markdown, and the `/research` and `/docs-audit` skills. Prefer those mechanisms
over adding prose here.
